03 - algoritmo: problema 03
programa {
  funcao inicio() {
    //Declaração de variáveis
    real nota1, nota2, nota3, nota4,media_aritmetica
    //Entrada de dados 
    escreva ("informe a primeira nota:")
    leia(nota1)
    escreva ("informe a segunda nota:")
    leia (nota2)
    escreva ("informe a terceira nota")
    leia (nota3)
    escreva ("informe a quarta nota")
    leia (nota4)

    //processamento
    media_aritmetica = (nota1+nota2+nota3+nota4)/4

    //Saida de dados 
    escreva ("A media é:",media_aritmetica)

  }
}


04 - algoritmo: problema 04
programa {
  funcao inicio() {
    //Declaração de variáveis 
    real janeiro,fevereiro,marco,media_Aritmetica

    //Entrada de dados
    escreva("Informe o valor gasto em janeiro")
    leia (janeiro)
    escreva("Informe o valor gasto em fevereiro")
    leia(fevereiro)
    escreva("Informe o valor gasto em março")
    leia(março)
   

    //Processamento 
    media_Aritmetica= (janeiro+fevereiro+marco)/3

    //Saída de dados 
    escreva("A média aritmética é", media_Aritmetica)

  }
}

05 - algoritmo: problema 05
programa {
  funcao inicio() {
    //Declaração de variáveis
    real altura,peso_ideal
    
    //Declaração de constante
    const real k=72.7
    const  inteiro b=58

    //Entrada de dados 
    escreva("Informe a altura")
    leia(altura)

    //Processamento
    peso_ideal= k*altura-b

    //Saída de dados
    escreva("O seu peso ideal é", peso_ideal)

  }
}

06 - algoritmo: problema 06
programa {
  função início() {
  //Declaração de variavel
    real x, y, troca

    //Entrada de dados 
    escreva("Informe o primeiro valor: ")
    leia(x)
    escreva("Informe o segundo valor: ")
    leia(y)

    //Processamento 
    troca = x 
    x = y 
    y = troca

    // Saida de dados
    escreva("O valor trocado de x ", x , " O valor trocado de y ", y)

 }
}


07 - algoritmo: problema 07
programa {
   função início() {
    //Declaração de variável
    real raio, diametro, comprimento, area

    //Entrada de Dados
    escreva("Informe o valor do raio: ")
    leia(raio)

    //Processamento
    diametro = raio * 2
    comprimento = 3.14 * diametro
    area = (raio * raio) * 3.14

    //Saida de Dados
    escreva("Com o raio ", raio , " o diametro sera " , diametro, " o comprimento sera ", comprimento, " e area sera ", area )

 }
}

08- algoritmo: problema 08
programa {
  funcao inicio() {
    // declaraçao de variaveis
    real celcius, fahrenheit, temperatura

    //entrada de dados
    escreva("informe a temperatura em celsius: ")
    leia(celcius)

    //processamento
    fahrenheit = celcius * 9.0/5.0 + 32

    //saida de dados
    escreva(" a temperatura em fahrenhat é: ", fahrenheit)

  }
}
